# Welcome to GoodReads!

GoodReads is a web app built on top of ReactJS.
It shows the trending news on different categories using the news API.

# The app won't render any news as the free news API is not supported in production
